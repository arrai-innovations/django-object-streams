# Optional local test database settings loaded by just.
#
# Blank HOST and PORT values let psycopg use the local PostgreSQL socket.
DATABASE_NAME=django_object_streams
DATABASE_USER=postgres
DATABASE_PASSWORD=
DATABASE_HOST=
DATABASE_PORT=
